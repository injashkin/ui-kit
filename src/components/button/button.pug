mixin btn(options = '', text = '')
  -
    let tag = 'btn'
    const flags = ['active', 'disabled', 'loading', 'circle']
    const arr = options.split(' ')
    const classItems = arr.map(item => {
      if (flags.includes(item)) return item
      return item ? `btn-${item}` : ''
    })
    let classes = classItems.join( ' ' )

  if (arr.includes('group'))
    - classes = 'btn-group'
    if (arr.includes('block'))
      - classes = 'btn-group btn-group-block'
    div(class=classes)
      block
  else 
    - classes = classes === '' || classes === ' ' ? 'btn' : `btn ${classes}`
    - const tabindex = arr.includes('disabled') ? '-1' : none
    button(class=classes tabindex=tabindex)= text
      block

  //- #{tag}(class=classes)= text